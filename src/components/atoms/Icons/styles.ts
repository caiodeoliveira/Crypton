import styled from "styled-components";

const TrendingIconContainer = styled.button`
  width: 100px;
  border: none;
  outline: none;
  background: none;
  line-height: none;
`;

const S = {
  TrendingIconContainer,
};

export default S;
